import React from "react";
import UserInfo from "../UserInfo/UserInfo";

function Header() {
    return (
        <div>
            Header
            <UserInfo />
        </div>
    );
}

export default Header;
